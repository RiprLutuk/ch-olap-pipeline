{
  "name": "clickhouse-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.clickhouse.ClickhouseSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
