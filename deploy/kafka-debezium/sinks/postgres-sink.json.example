{
  "name": "postgres-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.postgres.PostgresSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
