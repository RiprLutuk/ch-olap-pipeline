{
  "name": "rabbitmq-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.rabbitmq.RabbitmqSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
