{
  "name": "redshift-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.redshift.RedshiftSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
