{
  "name": "iceberg-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.iceberg.IcebergSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
