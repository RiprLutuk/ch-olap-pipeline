{
  "name": "duckdb-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.duckdb.DuckdbSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
