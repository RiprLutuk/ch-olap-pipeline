{
  "name": "elasticsearch-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.elasticsearch.ElasticsearchSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
