{
  "name": "snowflake-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.snowflake.SnowflakeSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
