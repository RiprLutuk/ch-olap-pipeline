{
  "name": "opensearch-sink",
  "config": {
    "connector.class": "com.example.kafka.connect.opensearch.OpensearchSinkConnector",
    "tasks.max": "1",
    "topics.regex": "${TOPICS_REGEX:-oltpdemo\\..*}"
  }
}
