{
  "name": "sqlite-source",
  "config": {
    "connector.class": "com.example.kafka.connect.sqlite.SqliteConnector",
    "tasks.max": "1",
    "topic.prefix": "${TOPIC_PREFIX:-oltpdemo}",
    "table.include.list": "${SQLITE_TABLE_INCLUDE}"
  }
}
