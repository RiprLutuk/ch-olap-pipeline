{
  "name": "rest-source",
  "config": {
    "connector.class": "com.example.kafka.connect.rest.RestSourceConnector",
    "tasks.max": "1",
    "topic.prefix": "${TOPIC_PREFIX:-oltpdemo}",
    "table.include.list": "${REST_TABLE_INCLUDE}"
  }
}
