{
  "name": "db2-source",
  "config": {
    "connector.class": "io.debezium.connector.db2.Db2Connector",
    "tasks.max": "1",
    "topic.prefix": "${TOPIC_PREFIX:-oltpdemo}",
    "table.include.list": "${DB2_TABLE_INCLUDE}"
  }
}
